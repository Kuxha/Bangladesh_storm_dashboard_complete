Flask==2.0.1
numpy==2.0.0         # wheel available for Py 3.12
pandas==2.2.2        # wheel for Py 3.12
setuptools==70.0.0   # ships its own distutils stub
# geopandas REMOVED – not needed at runtime
